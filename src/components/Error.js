import React from "react";

const Error = () => {

    return(
        <section id="content">
        <h2 className="subheader"><em>Página no encontrada, intente nuevamente</em></h2>
        <p><i>La página a la que intentas acceder no existe en la web</i></p>
        </section>
    );
}

export default Error;